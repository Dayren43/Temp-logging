export const SENSOR = {
	name: 'Living room',
};

// Backend API base (Express server). Matches the host used elsewhere in the app.
export const API_BASE = 'http://epsilon.local:3000';

export const COMFORT = {
	lo: 20,
	hi: 24,
};
